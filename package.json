{
	"name": "test",
	"module": "index.ts",
	"version": "1.3.5",
	"type": "module",
	"scripts": {
		"bump": "bun bump-version.ts",
		"dd": "bump --commit 'v%s' --tag '%s' manifest.json",
		"tt": "git push && git push --tags"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"version-bump-prompt": "^6.1.0"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
